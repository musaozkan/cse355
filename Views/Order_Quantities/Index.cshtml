@model IEnumerable<cse355.Models.Order_Quantities>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QuantityID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QuantityID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QuantityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
