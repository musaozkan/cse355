@model IEnumerable<cse355.Models.TruckDeliveryRoutes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VehicleID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryRoute)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VehicleID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryRoute)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeliveryRouteID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeliveryRouteID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeliveryRouteID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
